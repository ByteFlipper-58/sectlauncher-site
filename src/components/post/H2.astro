---
import Heading from './Heading.astro';

interface Props {
  id?: string;
  class?: string;
}

const { id, class: additionalClasses }: Props = Astro.props;
---

<Heading level={2} id={id} class={additionalClasses}>
  <slot />
</Heading>
